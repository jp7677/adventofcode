use crate::util::*;

#[test]
fn part01() {
    let input = read_input(DAYS::Day00);

    assert_eq!(input, "0");
}

#[test]
fn part02() {
    let input = read_input(DAYS::Day00);

    assert_eq!(input.lines().count(), 1);
}
